import Abstract from '../abstract.js';

export default class MenuControlsView extends Abstract {
  _getTemplate() {
    return (
      `<div class="trip-main__trip-controls  trip-controls">
      </div>`
    );
  }
}
